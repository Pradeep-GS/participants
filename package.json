{
    "name": "events-root",
    "version": "1.0.0",
    "description": "Root package.json to manage frontend",
    "scripts": {
        "dev": "npm run dev --prefix frontend",
        "install-frontend": "npm install --prefix frontend",
        "start": "npm install --prefix frontend && npm run dev --prefix frontend"
    }
}